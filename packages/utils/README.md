# @zed-ui/utils

Shared utilities (`cx`, `mergeRefs`) for Zed UI packages.

Installed automatically with `@zed-ui/react`. Application code should use `@zed-ui/react` exports.

```bash
npm install @zed-ui/react @zed-ui/themes react react-dom
```

## License

MIT
