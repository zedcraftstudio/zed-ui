name: Deploy docs to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - name: Build playground for GitHub Pages
        run: pnpm --filter @zed-ui/playground build:pages
        env:
          VITE_BASE_PATH: ${{ vars.PAGES_BASE_PATH || '/' }}
          VITE_STORYBOOK_URL: ${{ vars.STORYBOOK_URL || 'https://zed-ui.zedcraftstudio.com/storybook/' }}
      - run: pnpm --filter @zed-ui/docs build:pages
      - run: node scripts/deploy-pages.mjs
      - uses: actions/upload-pages-artifact@v3
        with:
          path: pages-dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
